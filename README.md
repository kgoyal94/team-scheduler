# Team Scheduler (Shift Board)

Café shift scheduling app — ported from `shift-board.html` prototype.

## Getting started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## References

- `CONTEXT.md` — living project state and ownership map
- `STRATEGY.md` — phased roadmap
- `shift-board.html` — original prototype (kept for reference)
