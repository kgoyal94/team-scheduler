NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Comma-separated allowlist of emails permitted to sign in.
# Leave blank in local dev to allow any email; set it in every environment with real data.
ALLOWED_EMAILS=
